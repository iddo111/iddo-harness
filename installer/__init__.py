"""Installer-side utilities that ship with the repo but are not part of the agent.

Kept a package so the CLIs are runnable as modules from a checkout:
``python -m installer.gen_keys``, ``python -m installer.policy_lint``.
"""
