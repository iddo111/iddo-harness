PyYAML>=6.0
click>=8.1
httpx>=0.27
pytest>=7
watchdog>=4.0.0
psutil>=5.9.0
# v3 Track A: the WebSocket transport (agent/ws_bridge.py). Optional — without
# it the harness falls back to git polling alone, which is slower but complete.
websockets>=12.0
# v3 Track B: Ed25519 result signing (agent/signing.py) and the Fernet-encrypted
# secrets vault (agent/secrets_vault.py). Without it the harness still runs —
# results go out unsigned, and a task referencing {{secret:...}} fails loudly
# instead of leaking a placeholder to a remote endpoint.
cryptography>=42.0.0
# v3 Track C: cron parsing for the scheduler (agent/scheduler.py). Optional — a
# built-in five-field parser answers the same questions when it is absent.
croniter>=2.0.0
